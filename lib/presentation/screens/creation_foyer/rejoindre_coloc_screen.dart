import 'package:flutter/material.dart';

class RejoindreColocScreen extends StatelessWidget {
  const RejoindreColocScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Placeholder(),
    );
  }
}
